# 一多 OS 原型机规划：唤醒数字生命的第一步

> 再完美的创世宣言，也得有个实际的载体来证明它的威力！

---

## 相关文档导航

| 文档 | 内容 | 位置 |
|------|------|------|
| **本文档** | 原型机的实现规划 | [原型机规划.md](./原型机规划.md) |
| **011.生物学构架.md** | 创世哲学与四元架构核心思想 | [011.生物学构架.md](./操作系统/011.生物学构架.md) |
| **012.生命学构架应用.md** | 实用 Checklist 与实施指南 | [012.生命学构架应用.md](./操作系统/012.生命学构架应用.md) |
| **007.一多 OS 后端架构.md** | 完整实现指南 | [007.一多 OS 后端架构...](./interfaces/技术文档/007.一多 OS 后端架构：站在 Wasm WASI 巨人肩膀上的可编程调度系统.md.md) |

---

## 原型机目标

**最小可用，但要完整跑通闭环：**

```
意志（配置文件）→ DNA（器官/细胞）→ 生长路径（树形）→ 血液系统（链式）
```

### 核心演示场景

我们选择**"智能相机场景"**作为第一个原型演示，因为它简单、直观，且能涵盖多个架构要点：

1. 用户通过配置文件（意志）指定想要什么能力
2. 系统自动加载合适的细胞（DNA）
3. 细胞调用后端适配器（UniHAL）
4. 系统运行并输出结果

---

## 阶段一：MVP-1 (Mock-only，1-2周)

### 目标：跑通最小闭环

**不依赖任何真实硬件或库，全部 Mock**

#### 目录结构

```
yiduo/
├── main.mbt                          # 入口
├── core/
│   └── types.mbt                     # 核心类型定义
├── adapters/
│   └── camera/
│       └── mock_camera.mbt          # Mock 相机后端
├── cells/
│   └── camera_cell.mbt               # 相机细胞（标准接口）
├── configs/
│   └── smart_camera.yaml            # 场景配置（意志）
└── scheduler/
    └── simple_scheduler.mbt         # 简易调度器
```

#### 核心实现

1. **配置解析器** - 读取 YAML 配置（意志）
2. **简易调度器** - 加载需要的细胞和后端
3. **Mock 后端** - 模拟相机功能
4. **简单细胞** - 标准接口封装

#### 演示效果

运行命令：
```bash
moon run main configs/smart_camera.yaml
```

输出：
```
一多 OS 数字生命体启动...

🎯 加载配置文件（意志）：smart_camera.yaml
   ├── 需要能力：相机
   └── 优先后端：mock

🧬 加载细胞：camera_cell
   └── 选择后端：mock_camera

🩸 血液系统启动...

📸 相机捕获图像：MockImage-1 (640x480)
📸 相机捕获图像：MockImage-2 (640x480)
📸 相机捕获图像：MockImage-3 (640x480)

✅ 数字生命体运行成功！
```

---

## 阶段二：MVP-2 (加入更多后端，2-3周)

### 目标：展示调度能力

**加入多个可选后端，展示智能选择和降级**

#### 新增内容

1. **更多后端选项**
   - `opencv_camera.mbt` (Mock OpenCV)
   - `libcamera_camera.mbt` (Mock Libcamera)

2. **调度策略配置**
   - 优先性能
   - 优先兼容
   - 优先Mock（测试）

3. **自动降级演示**
   - 如果 libcamera 不可用，自动降级到 OpenCV
   - 如果 OpenCV 也不可用，降级到 Mock

#### 演示效果

```bash
moon run main configs/smart_camera.yaml --strategy prefer_performance
```

输出：
```
一多 OS 数字生命体启动...

🎯 加载配置...
   └── 策略：优先性能

🔍 尝试后端：libcamera (优先级最高)
   └── ❌ 后端不可用（Mock）

🔄 降级：尝试后端：opencv
   └── ✅ 后端可用

🧬 加载细胞...
   └── 实际后端：opencv

📸 相机捕获图像：OpenCVImage-1 (1920x1080)
...
```

---

## 阶段三：MVP-3 (完整场景组合，3-4周)

### 目标：展示细胞组合能力

**加入多个细胞，展示场景组合配置**

#### 新增内容

1. **更多细胞类型**
   - `audio_cell.mbt` - 音频细胞
   - `display_cell.mbt` - 显示细胞

2. **多场景配置**
   - `music_producer.yaml` - 音乐制作人场景
   - `iot_gateway.yaml` - IoT 网关场景
   - `game_platform.yaml` - 游戏平台场景

3. **细胞间通信**
   - 简易事件总线（链式血液系统）

---

## 技术选择

### 语言与平台

- **MoonBit** - 主力开发语言（已验证可用）
- **Wasm/WASI** - 未来方向（暂不立即实现）

### 配置格式

- **YAML** - 人眼友好，用于场景配置
- **JSON** - 内部数据交换（可选）

### 依赖管理

- **Moon 工具链** - 官方包管理
- **最小依赖原则** - 核心功能尽量自包含

---

## 实施原则

### 1. 自底向上，但保持顶层设计清晰
- 先实现底层类型和后端
- 再实现调度和细胞
- 最后接入配置文件

### 2. Mock 优先，真实后加
- 先全部用 Mock 跑通
- 再逐步接入真实硬件
- 保持接口稳定

### 3. 文档优先，可追溯
- 每部分代码关联对应概念
- 代码注释引用文档章节
- 便于理解和维护

---

## 下一步行动

### 立即开始（今天）
- [ ] 清理现有项目结构，保留文档
- [ ] 创建阶段一的目录结构
- [ ] 实现核心类型定义 (`core/types.mbt`)

### 近期（本周）
- [ ] 实现 Mock 相机后端
- [ ] 实现简单相机细胞
- [ ] 实现简易配置解析器
- [ ] 跑通第一个端到端流程

### 里程碑
- **MVP-1** 完成：✅ 可以展示"意志→DNA→器官"闭环
- **MVP-2** 完成：✅ 可以展示调度和降级能力
- **MVP-3** 完成：✅ 可以展示细胞组合配置

---

## 预期成果

完成这个原型机后，我们将拥有：

1. **可运行的演示** - 用事实证明这套理念
2. **可扩展的骨架** - 后续可以接入真实硬件
3. **清晰的文档** - 概念到实现的完整映射
4. **信心** - 证明这套范式是可行的

**让我们开始唤醒第一个数字生命吧！** 🚀
