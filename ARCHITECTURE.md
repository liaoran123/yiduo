# 一多 OS 架构设计文档

## 核心思想

一多 OS 采用**四层概念体系**进行设计。这是一个**逻辑分层模型**，帮助我们理解和沟通，不直接对应到四个固定的包。

```
┌─────────────────────────────────────────────────────────────────┐
│  第1层：细胞组合配置 (App Scenarios)                             │
│  - 根据场景动态选择和组合计算细胞                               │
│  - YAML 配置驱动                                                │
└─────────────────────────────┬───────────────────────────────────┘
                              │
┌─────────────────────────────┴───────────────────────────────────┐
│  第2层：调度整合 (Scheduler & Orchestrator)                     │
│  - 智能选择合适的后端实现                                       │
│  - 策略配置与降级容错                                           │
└─────────────────────────────┬───────────────────────────────────┘
                              │
┌─────────────────────────────┴───────────────────────────────────┐
│  第3层：计算细胞 (Compute Cells)                                │
│  - 标准 WIT 接口定义                                            │
│  - 功能独立的模块                                               │
└─────────────────────────────┬───────────────────────────────────┘
                              │
┌─────────────────────────────┴───────────────────────────────────┐
│  第4层：后端适配器 (Backend Adapters)                           │
│  - 对接现有成熟库                                               │
│  - Mock、libcamera、OpenCV 等                                   │
└─────────────────────────────────────────────────────────────────┘
```

## 实际目录结构（功能导向，不是严格的四层）

```
yiduo/
├── interfaces/                      # WIT 接口定义（核心）
│   ├── base/
│   ├── env/
│   └── cap/
│       ├── camera/
│       ├── audio/
│       └── ...
├── runtime/                         # 运行时引擎
│   ├── scheduler/                   # 调度逻辑（第2层）
│   ├── cell_manager/                # 细胞管理（第3层）
│   ├── backend_manager/             # 后端管理（第4层）
│   └── ...
├── adapters/                        # 后端适配器实现（第4层）
│   ├── camera/
│   │   ├── mock/
│   │   ├── libcamera/
│   │   └── opencv/
│   ├── audio/
│   └── ...
├── scenarios/                       # 应用场景配置（第1层）
│   ├── smart_camera.yaml
│   ├── music_producer.yaml
│   └── ...
├── configs/                         # 通用配置
│   ├── tech-stacks/
│   └── app-scenarios/
├── examples/                        # 示例应用
│   └── hello/
└── docs/                            # 文档
```

## 四层在代码中的映射

### 第1层：细胞组合配置
- **实现位置**：`scenarios/` 目录下的 YAML 文件
- **相关代码**：`runtime/scenario_loader.mbt` 等
- **责任**：
  - 定义特定场景需要哪些细胞
  - 配置每个细胞的策略和优先级
  - 提供场景模板和自定义能力

### 第2层：调度整合
- **实现位置**：`runtime/scheduler/` 目录
- **相关代码**：
  - `backend_selector.mbt` - 后端选择逻辑
  - `policy_engine.mbt` - 策略引擎
  - `health_monitor.mbt` - 健康检查与降级
- **责任**：
  - 根据策略选择最佳后端
  - 处理降级和恢复
  - 监控系统健康

### 第3层：计算细胞
- **实现位置**：`runtime/cells/` 目录，或外部 Wasm 模块
- **核心定义**：`interfaces/cap/` 下的 WIT 接口
- **责任**：
  - 定义标准能力接口
  - 提供内置细胞的参考实现
  - 细胞隔离和安全沙箱

### 第4层：后端适配器
- **实现位置**：`adapters/` 目录
- **责任**：
  - 每个能力有多个后端实现
  - 提供 Mock 实现用于开发测试
  - 对接现有成熟库

## 概念与实现的对应关系

| 概念分层 | 主要对应的目录 | 说明 |
|---------|--------------|------|
| 细胞组合配置 | `scenarios/` + `configs/` | 配置文件和加载逻辑 |
| 调度整合 | `runtime/scheduler/` | 核心调度逻辑 |
| 计算细胞 | `interfaces/cap/` + `runtime/cells/` | 接口定义 + 内置实现 |
| 后端适配器 | `adapters/` | 各种后端实现 |

**注意**：一个代码模块可能参与多个概念层，没有严格的边界。

## 实现原则

1. **先有接口，后有实现** - 优先定义 WIT 标准
2. **Mock 优先** - 每个能力先提供 Mock 后端，让系统能跑起来
3. **配置驱动** - 用户通过 YAML 配置就能改变行为
4. **向后兼容** - 接口稳定，后端可替换

## 下一步实现计划

1. ✅ **Hello World** - 项目能编译运行（已完成）
2. 🔄 **核心类型与错误处理**
3. 🔲 **Mock 后端框架**
4. 🔲 **场景加载器**
5. 🔲 **简单示例**
