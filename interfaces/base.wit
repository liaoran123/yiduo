// 所有硬件操作的返回状态
type result<T, E> = {
  ok: T,
  err: E,
}

// 核心错误类型
enum Error {
  Success,
  InvalidArg,
  IoError,
  PermissionDenied,
}

// 日志接口
interface log {
  log: func(msg: string) -> result<unit, Error>
}

// 时间接口
interface time {
  current_time: func() -> result<u64, Error>
}